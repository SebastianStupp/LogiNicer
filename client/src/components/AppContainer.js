import styled from '@emotion/styled';

const AppContainer = styled.div`
  width: 100vw;
  height: 100vh;
  background: linear-gradient(#0d3955 60%, black);
  display: flex;
  flex-direction: column;
`;

export default AppContainer;
